# Copia este archivo como ".env" en la raíz del proyecto y ajusta valores.
# PostgreSQL (misma base que usan los scrapers y web_app.py)

DB_USER=postgres
DB_PASSWORD=tu_contraseña
DB_HOST=127.0.0.1
DB_PORT=5432
DB_NAME=farmacia_scraper_db
